import 'package:flutter/material.dart';

// All the constant values we use throughout the project are added to this file.
// There is a standard in flutter where we start a constant with k.
const kTextColor = Color(0xFFFF8888);
const kOutputTextColor = Color(0xFFFF5555);

const kInputLabelStyle = TextStyle(
                          color: kTextColor, 
                          fontSize: 50, fontWeight: 
                          FontWeight.bold);

const kOutputLabelStyle = TextStyle(
                          color: kOutputTextColor, 
                          fontSize: 60, fontWeight: 
                          FontWeight.bold);             