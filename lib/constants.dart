import 'package:flutter/material.dart';

// All the constant values we use throughout the project are added to this file.
// There is a standard in flutter where we start a constant with k.
const kTextColor = Color(0xFFFF8888);